# End-to-end test pass — 2026-09-06

Run against the live demo (https://syon4u.github.io/queueflow-demo/) and the
`diadwozorwkzhexhjfgb` Supabase project, on top of the `audit/blockers`
series. Public flows were driven anonymously in a clean browser; staff/admin
flows as the test admin in Chrome. Every fix below was redeployed to the demo
and re-verified live.

## Flows covered

| Flow | Result |
|---|---|
| Booking (/customer) | **Was broken for every anonymous visitor** (RLS) — fixed, verified |
| Kiosk walk-in (/kiosk) | Was broken — fixed, verified (ticket, position, QR) |
| Virtual queue join + check-in (/virtual-queue) | Was broken + fake position — fixed, verified |
| Check-in by code (/check-in) | Was broken — fixed, verified (position #1) |
| Status by code (/status) | Was broken — fixed, verified |
| Lookup + cancel (/appointment-lookup) | Was broken — fixed, verified |
| Mobile queue (/mobile-queue?appointment=) | Was broken (401) + mock data — fixed, verified |
| Digital signage (/digital-signage) | Was broken (401, bad location id) — fixed, verified |
| Staff: Call Next / Mark Served / No-Show | Verified |
| Staff: Undo | Never appeared (two bugs) — fixed, re-verify after deploy |
| Staff: Documents upload | Verified (file stored in private bucket, listed) |
| Real-time queue update across browsers | Appears within the 30 s poll; push not separately confirmed |
| Admin tabs (all 10) | Throttling crashed the whole app — fixed; all render |
| Power user: user administration | Renders; role changes not exercised (real accounts) |
| Power user: location / service CRUD | Buttons were stubs — real CRUD mounted; re-verify |
| Performance report | Renders; "Today" metric definitions unverified |
| Lookup by name + phone | Server-side exact match (was substring/`includes`) |

## Findings

| # | Flow | Page | Finding | Severity | Status |
|---|---|---|---|---|---|
| F1 | Copy | /customer, /new-appointment, /appointment-lookup | PageLayout headerTitle "Consumer Protection Division" / "Schedule your appointment with Broward County services" still in source; en/landing.json + es/pt/ht headerSubtitle mention Broward | Credibility | open |
| F2 | Copy | booking steps | AppointmentStepContent "Select the Broward County office", AppointmentStepHeader "Broward County services", TestimonialsSection location 'Broward County' | Credibility | open |
| F3 | Kiosk | /kiosk | Only 6 of 14 locations listed (8 are queue_status=closed); mixed NY/FL demo addresses | Polish | open |
| F4 | Console | first load | One "Failed to load resource 404" on initial page load, source not yet identified | Polish | open |
| F5 | Booking/kiosk/virtual queue/check-in/status | /customer, /kiosk, /virtual-queue, /check-in, /status, /appointment-lookup | All anonymous flows read/write customers+appointments directly; RLS (no anon SELECT/UPDATE) makes every INSERT...RETURNING fail ("new row violates row-level security policy"), every lookup return nothing, and check-in's UPDATE silently rejected. Booking form still showed a success toast. | **Blocker** | fixed: SECURITY DEFINER RPCs (migration 20260906190000) + client refactor via src/lib/publicQueue.ts |
| F6 | Data | DB | 12 checked_in + ~25 scheduled rows from 2025 never resolved; new walk-in reported "#2 in line" behind a 2025 customer | Credibility | fixed: 24h queue window + stale rows auto-closed as no_show (migration 20260906191000) |
| F7 | Staff | /staff Queue Overview | Scheduled (not arrived) booking rendered as "Waiting -59 min" and counted in "Currently Waiting" | Polish | open |
| F8 | Staff | /staff | Call Next served the checked-in walk-in (correct), toast shown; page takes ~10-20s to first paint in Chrome | Polish (perf) | open |
| F9 | Virtual queue / mobile queue | /virtual-queue, /mobile-queue | Hardcoded fake position (5 / 25 min) and mock progress (3 of 8) | Credibility | fixed: wired to RPC position/wait |
| F10 | Admin | /admin Throttling tab (+ staff CommunicationDialog, AddCustomerForm) | `<SelectItem value="">` throws in Radix → whole app unmounts to a blank page; no ErrorBoundary anywhere | **Blocker** | fixed: sentinel values + app-level ErrorBoundary |
| F11 | Data | locations.current_capacity | Trigger only released a seat on leaving checked_in; served customers (checked_in→in_progress→completed) leaked forever (Main Office 2/50 with 1 in queue) | Credibility | fixed: trigger + recompute (migration 20260906192000) |
| F12 | Kiosk | /kiosk | Walk-in flow works end-to-end after fix: ticket #, position, QR, 30s auto-reset | — | verified |
| F13 | Check-in / Status | /check-in, /status | Lookup by APT code, confirm, check in → position #1; status shows Scheduled/Checked-in with ticket number | — | verified |
| F14 | Staff | /staff | Call Next → Now Serving (walk-in first), Mark Served → Served Today 1; no Undo control surfaced on the dashboard after serve (UndoActionButton exists in StaffMainContent) | Polish | open |
| F15 | Signage | /digital-signage | "Connection Error": fetches queue-stats edge function with the (absent) user session as bearer → 401; also hardcoded location id …0000 that does not exist | Blocker (for lobby screens) | fixed: public_signage_board RPC + ?location= / first open location |
| F16 | Mobile queue | /mobile-queue?appointment=… | "Unable to load queue information": queue-position edge function 401 for anon | Blocker | fixed: RPC by appointment id |
| F17 | Lookup | /appointment-lookup | Find by APT/CUST code works; Cancel works (window.confirm); status label rendered raw "Checked_in" | Polish | fixed label |
| F18 | Virtual queue | /virtual-queue | Join works: ticket APT-…, expected position, est. wait from live snapshot; check-in button wired to RPC | — | verified |
| F19 | Admin | /admin tabs | Dashboard, Analytics, Communication, Capacity, SMS Commands, Queue Flow 2.0, Settings, Security, Health Check all render; Throttling crashed (F10) — re-verify after deploy | — | partial |
| F20 | Reports | /performance-report | Renders; "Today" summary shows Total Appointments 36 / Completed 0 while 1 was served today — metric definitions unverified | Polish | open |
| F21 | Signage | /digital-signage | Renders after fix: first open location (Airport Kiosk), stats, capacity, 10s refresh; ?location=<uuid> supported | — | verified |
| F22 | Mobile queue | /mobile-queue?appointment=… | Renders after fix: ticket, In Queue, position, est/current wait; "people ahead" arithmetic was total-position (fixed to position-1) | — | verified |
| F23 | Admin | /admin Throttling | Renders after Select fix (no crash) | — | verified |
| F24 | Staff | /staff after fixes | Queue Overview: Walkin Completed, Tester Waiting 14 min, Kiosk Waiting 10 min; cancelled virtual entry hidden; Currently Waiting 2 | — | verified |
| F25 | i18n | /customer (Español) | Language switcher changes label but the booking form stays English (hardcoded strings) | Credibility | open — pricing bullet reworded to "partial coverage today" |
| F26 | Power user | /power-user Locations & Services | Create/Edit/Delete Location and Service were stubs ("form would open here" toast); admin LocationsTab/ServicesTab with real CRUD existed but were unmounted | Blocker (for CRUD) | fixed: mounted the real components |
| F27 | Data | locations | 11 location emails still …@broward.org (visible in power-user table) | Credibility | fixed: migration 20260906194000 |
| F28 | Staff | /staff Undo | staff_actions had RLS policies but no table GRANT for authenticated → every logAction insert failed silently → Undo never rendered; undo also needed UPDATE | Blocker (Undo) | fixed: migration 20260906195000 — re-verify |
| F29 | Real-time | /staff | Kiosk walk-in from another browser appeared on the staff dashboard without reload within ~30s (poll interval 30s; push subscription not separately confirmed) | — | verified |
| F30 | Staff | /staff No-Show | Call Next → No-Show marks the served customer no-show; No-Shows counter 1 | — | verified |
| F31 | Power user | Locations & Services → Add New | Create Location dialog (name, address, phone, email, max capacity, buffer) creates the row ("Location created successfully"); test row deleted afterwards | — | verified |
| F32 | Staff | /staff Undo (root cause 2) | useStaffActions read the user from MinimalAuthContext, which main.tsx provides as `{ user: null }` — logAction always returned early. Switched to useAuth(). | Blocker (Undo) | fixed |
| F33 | Staff | Documents | Upload of a PNG lands in the private `appointment-documents` bucket and is listed ("1 of 1 file(s) uploaded") | — | verified |
| F34 | Power user | dashboard | "Today's Appointments 0" while /staff and /admin count 4–5 today — a third, inconsistent definition of "today" | Polish | open |
| F35 | Power user | Locations → row trash icon | Delete works ("Location deleted successfully"); both test rows removed via UI | — | verified |
| F36 | Power user | Locations → Add New | After a successful create the dialog stayed open long enough for a second submit to create a duplicate (closeDialog runs in onSuccess after the refetch) | Polish | open |
| F37 | Staff | /staff Undo | After both fixes: Call Next → "Undo: Called customer" button appears; clicking it reverts the appointment to checked_in (realtime check-in notification fires) | — | verified |

| F38 | Staff | /staff Undo | The undo itself is logged as an undoable action ("Undo: Last action" appears after undoing) and the original action's `undone_at` is not set; the dashboard row also keeps "Now Serving" until the next 30 s poll | Polish | open |

## Database migrations added in this pass (all applied to the live project)

| File | Purpose |
|---|---|
| 20260906190000_public_queue_rpcs.sql | SECURITY DEFINER functions for all anonymous customer flows |
| 20260906191000_queue_window_and_stale_cleanup.sql | 24h queue window; stale 2025 rows auto-closed as no_show |
| 20260906192000_fix_location_capacity_trigger.sql | Capacity trigger releases seats on leaving in_progress; counters recomputed |
| 20260906193000_public_signage_board.sql | Lobby board RPC (first name + initial only) |
| 20260906194000_neutralize_location_emails.sql | 11 @broward.org location mailboxes → neutral demo addresses |
| 20260906195000_staff_actions_grants.sql | GRANTs + UPDATE policy so staff action logging / undo work |

Test rows left in the database are tagged `notes = 'Demo seed data'` (customers E2E Tester / Walkin / Kiosk / Virtual / Realtime / Undo) and can be deleted.

## Still open (not fixed in this pass)

- Booking form and most staff/admin screens are English-only under Español / Português / Kreyòl (F25); pricing copy now says "partial coverage today".
- "Today" is defined three different ways across /staff, /admin and /power-user (F20, F34).
- Location create dialog can be double-submitted (F36).
- Placeholders still to replace with real values: `sales@queueflow.app`, `support@queueflow.com`, footer support hours, PWA icons.
- Seeded services remain consumer-protection names pending a vertical decision.
- 255 pre-existing lint findings; CI runs lint with continue-on-error.
- `audit/blockers` cannot be pushed until the GitHub token has the `workflow` scope (`gh auth refresh -h github.com -s workflow`).
